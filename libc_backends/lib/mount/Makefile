PKGDIR		?= ../..
L4DIR		?= $(PKGDIR)/../../..

TARGET		= libmount.a
LINK_INCR       = libmount.a
PC_FILENAME     = libmount
SRC_CC          = mount.cc
CXXFLAGS        = -fno-exceptions

include $(L4DIR)/mk/lib.mk
