-- vim:set ft=lua:

local L4 = require("L4")
local l  = L4.default_loader;
local prio = 0xa0;
local sch = L4.Env.user_factory:create(L4.Proto.Scheduler, prio + 0x10, prio);

l:startv(
{
  scheduler = sch,
  log = {"test", "b"},
}, "rom/test_scheduler");
