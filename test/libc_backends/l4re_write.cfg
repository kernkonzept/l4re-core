-- vim:set ft=lua: -*- Mode: lua -*-

local t = require("rom/test_env")
local L4 = require("L4")

local l = L4.default_loader
local vcon = l:new_channel()

l:start(
  {
    caps = {
      vcon = vcon:svr()
    },
    log = {"test_l4re_write_tag", "green"}
  },
  "rom/" .. t.TEST_PROG
)
