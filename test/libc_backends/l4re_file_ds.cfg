-- vim:set ft=lua: -*- Mode: lua -*-

local t = require("rom/test_env")
local L4 = require("L4")

local l = L4.default_loader

l:start(
  {
    caps = {
      rwfs = L4.Env.rwfs,
      new_rw = l:create_namespace({}):m("rw"),
    },
    log = {"test_l4re_file_tag", "red"}
  },
  "rom/" .. t.TEST_PROG
)
